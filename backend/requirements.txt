Django==5.2.1
djangorestframework==3.15.2
django-cors-headers==4.4.0
whitenoise==6.7.0
gunicorn==22.0.0
dj-database-url==2.2.0
# psycopg2-binary is only needed when DATABASE_URL points to PostgreSQL (i.e. on Render).
# Install manually on production: pip install psycopg2-binary
# On Windows local dev with SQLite, this line can be commented out.
# psycopg2-binary==2.9.9
